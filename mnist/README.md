To run the without reconstruction error version:
```
python mnist-recon.py false
```
To run the with reconstruction error version:
```
python mnist-recon.py true
```
To run the strokes version:
```
python mnist-draw.py
```
